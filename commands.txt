1. minikube start --driver=docker

2. minikube status

finding an ip address which was assigned by kubernetes. 
3. minikube ip
192.168.49.2

4. minikube ssh --> no need pass an ip address.

--------------------------------------------------------
5.
Default password for the minikube node user credentials:
Username: docker
Password: tcuser

6. kubectl cluster-info

7. kubectl get nodes -> gives available nodes. 

8. kubectl get pods -> gives available pods. 

9. kubectl get namespaces -> used to group different resources.

10. List systems pods which are running in master node:
kubectl get pods --namespace=kube-system

11. Creating 
kubectl run nginx --image=nginx

12. 
kubectl describe pod nginx 